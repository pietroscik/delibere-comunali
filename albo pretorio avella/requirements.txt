# Core dependencies with precise version pinning for reproducibility
beautifulsoup4==4.12.3
openpyxl==3.1.5
pandas==2.2.3
Pillow==10.4.0
pypdfium2==4.30.0
pytesseract==0.3.13
python-dateutil==2.9.0.post0
python-dotenv==1.0.1
requests==2.32.3
XlsxWriter==3.2.0

# Configuration and validation
pydantic==2.8.2
pydantic-settings==2.4.0

# Testing
pytest==8.3.2
pytest-cov==5.0.0
pytest-asyncio==0.23.8

# Logging enhancements (included in stdlib, but documenting usage)
# Python stdlib logging with RotatingFileHandler

# Performance and caching
cachetools==5.5.0

# Optional: FAISS for RAG (uncomment if needed)
# faiss-cpu==1.8.0.post1

# Optional: Streamlit for dashboard (uncomment if needed)
# streamlit==1.37.0

# Optional: Google Generative AI (uncomment if needed)
# google-generativeai==0.7.2
